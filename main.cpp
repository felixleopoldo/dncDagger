#include <iostream>

#include <bits/stdc++.h>
#include "smc_stuff.cpp"

int main(int argc, char const *argv[])
{
  std::vector<int> order = {5, 3, 1, 2, 7, 6, 0, 4};
  //PrintVector(order);
  //move_element(order, 5, 2);
  //PrintVector(order);
  //std::swap(order[4], order[5]);

  PrintVector(order);

  //

  return 0;
}
